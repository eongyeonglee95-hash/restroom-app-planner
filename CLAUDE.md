# 참지마 (Chamjima)

급한 순간에 가장 가까운 화장실과 지금 문 연 약국을 찾아주는 지도 기반 앱.

## 필수 규칙

- 모든 구현은 `참지마_기획서.md`를 기준으로 한다. 작업 시작 전에 반드시 읽을 것.
- 기획서와 다르게 구현해야 할 상황이면 먼저 사용자에게 확인하고, 결정되면 기획서의
  해당 섹션과 `변경 이력`을 업데이트한다.
- 기획/기능 논의가 나오면 `.claude/skills/restroom-app-planner` 스킬의 규칙에 따라
  기획서를 관리한다 (하나의 문서를 계속 갱신, 변경 이력 기록).

## 아키텍처 요약 (상세는 기획서 참조)

- React 프론트 + Spring Boot 백엔드
- 지도: 네이버/카카오맵 API, 데이터: 공공데이터 API (화장실 + 심야/휴일 약국)
- DB: 내부 id(PK) + external_id(공공데이터 매칭용) + status(활성/폐쇄) 패턴
- 조회는 비로그인, 리뷰/즐겨찾기만 소셜 로그인(OAuth + JWT)
- 화면: 상단 탭 [급똥] / [약국]

## MVP 범위

지도 표시 + "지금 급해!" 버튼(도보 시간순 정렬) + 급똥/약국 탭. 그 외 기능은 MVP 이후.
