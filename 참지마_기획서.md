# 참지마 기획서

## 한줄 소개 / 배경

과민성 대장 증후군이 있는 남편과, 화장실을 자주 급하게 찾는 7살 쌍둥이를 위해 시작된
아이디어. 급한 순간에 가장 가까운 화장실과 지금 문 연 약국을 빠르게 찾아주는 지도 기반 앱.

## MVP

1. 가장 가까운 화장실 찾기 — 지도 위에 현재 위치와 주변 화장실(공중화장실, 지하철,
   백화점, 대형마트, 편의점, 카페, 병원) 전부 표시. "🚽 지금 급해!" 버튼 누르면 도보
   시간순 정렬.
2. 상단 내비게이션 탭 — [급똥] / [약국] 두 탭으로 지도 레이어 전환. 급똥 탭은 화장실
   지도, 약국 탭은 지금 영업 중인 약국만 표시.
3. **서비스 지역: 서울시 한정으로 시작** (2026-08 결정). 공공데이터 커버리지가 서울시
   기준으로 가장 안정적이라, 좁은 지역에서 확실하게 동작하는 걸 우선함. 지역 확장은
   MVP 이후 지자체별 데이터 추가로 진행.

## 핵심 기능

1. **화장실 상세 정보** — 깨끗함, 휴지 있음, 비데 있음, 줄 거의 없음, 24시간 여부 등을
   별점과 함께 표시.
2. **개방 시간 / 접근 제한 표시** — 공원 화장실 야간 잠김, 백화점 영업시간 제한, 지하철
   개찰구 안 위치 등 실제로 못 쓰는 상황을 미리 알려줌.
3. **아이와 함께 필터** — 유아 변기, 기저귀 교환대, 가족 화장실 유무로 필터링.
4. **IBS 모드** — "😭 급해!!" 버튼 하나로 첫 화면에 **가장 가까운 화장실 카드 딱 1개**만
   크게 보여주고 바로 내비게이션 시작. 리스트를 먼저 보여주지 않는 게 원칙 — 고민 없이
   3초 안에 "여기로 가면 된다"를 알 수 있어야 함. 카드 아래 "다른 화장실 보기"를 눌러야
   나머지 리스트가 펼쳐짐 (2026-08 화면 구조 재설계).
5. **화장실 신뢰도(사용자 리뷰)** — 별점 + 깨끗함/휴지/냄새 등 항목별 코멘트를 사용자들이
   계속 업데이트. 로그인 없이 누구나 작성 가능 (2026-08 변경 — 원래는 로그인 필요였으나,
   로그인 붙이는 비용 대비 얻는 게 적다고 판단해 제거). 작성은 3초 안에 끝나야 함:
   이모지 3단계(😀좋았어요 / 😐보통 / 😱별로였어요) + 체크박스(휴지 있음/비데 있음/
   줄 없음/무료/깨끗함/비밀번호 없음)만 고르면 끝. 리스트에는 긴 텍스트 대신 AI가
   👍/👎로 요약해서 보여줌 (예: 👍휴지 항상 있음 👍깨끗함 👎냄새 약간). 여기에 더해
   **자유 텍스트 한마디(선택, 최대 200자)** 도 남길 수 있음 (2026-08 변경 — 처음엔
   "3초 룰"에 맞춰 자유 텍스트를 빼고 체크박스만 두었으나, 사용자 요청으로 다시 허용.
   필수 항목이 아니라 3초 룰 자체는 유지되고, 여유 있는 사람만 선택적으로 상세 코멘트를
   남기는 구조). 상세 카드에는 최근 코멘트 있는 리뷰 최대 3개를 이모지+본문+작성
   시간으로 미리보기.
6. **SOS 초단위 표시** — "도보 2분" 대신 "58초", "1분 20초"처럼 초 단위로 다급함에 맞게
   표시. 색상은 🟢1분 이내 / 🟠3분 이내 / 🔴5분 이상 3단계만 사용 (불필요한 색 제거).
7. **급똥 경로 추천** — 계단 적은 길, 엘리베이터, 최단 경로 우선으로 내비게이션.
8. **즐겨찾기** — 자주 가는 화장실(회사, 집, 학원) 저장.
9. **지금 열려있는 약국** — 심야약국/휴일지킴이약국 공공데이터 기반, "현재 영업 중"
   필터가 기본. 화장실 다녀와도 계속 안 좋을 때 지사제 살 수 있는 약국을 바로 안내.
   상단 [약국] 탭에서 지도 레이어로 표시.
10. **급똥지수** — 별점보다 앞세워서 강조하는 종합 점수 (예: "🚽 급똥지수 97"). 거리·
    청결·휴지 여부·최근 후기·혼잡도·성공률을 반영해 계산, AI가 산출한 점수처럼
    보이게 표시 (2026-08 추가).

## 차별화 포인트

- "화장실판 네이버지도" 컨셉 — 지역명 검색 시 화장실만 필터링해서 표시 (비데, 유아변기,
  24시간, 장애인, 무료, 깨끗함, 여자줄/남자줄 짧음 등 체크박스 필터). 지역명/장소명 검색
  자체는 카카오 키워드 검색으로 구현됨 (2026-08) — 체크박스 필터는 아직 미구현.
- 🚨 긴급 접근 가능성 팁 — 편의점(직원에게 말하면 사용 가능), 스타벅스(구매 안 해도 가능),
  맥도날드(24시간), 백화점(지하 1층 추천), 지하철(개찰구 밖에도 있음) 같은 실사용 팁 제공.
  다른 화장실 앱들이 거의 다루지 않는 부분. 편의점/카페/대형마트는 카카오 로컬
  카테고리 검색으로 지도에 "이용 팁" 레이어(🟡)로 구현됨 — 공공데이터 기반 "검증된
  화장실"(🔵)과 구분해서 표시 (2026-08). 정부 표준데이터가 민간 시설(백화점/카페 등)을
  다루지 않아서 나온 보완책이기도 함.
- 💊 화장실 + 약국 결합 — "급한 순간 대응"이라는 하나의 컨셉으로 화장실과 지금 문 연
  약국을 함께 제공. 경쟁 서비스는 화장실 찾기에만 머물러 있음.

## 포지셔닝

- **주 포지셔닝: 급함** — 설치 동기는 절박한 순간. 메인 카피도 급함 계열로
  (예: "참지 마세요, 3초 안에 찾아드려요"). 이름 "참지마"와 일치.
- **보조 포지셔닝: 안심 (아이와 함께)** — 리텐션 담당. "아이와 함께 모드"를 켜면
  유아변기 + 기저귀 교환대 + 청결 별점 높은 곳만 지도에 표시.
- **안심 화장실 뱃지** — 조건을 충족하는 화장실에 뱃지 부여. 추후 지자체 인증 화장실
  데이터와 연계 가능.
- 원칙: 마케팅 메시지는 "급함" 하나로 통일하고, 안심은 기능 이름으로 살린다
  (포지셔닝 두 개를 동시에 밀면 메시지가 흐려짐).

## 경쟁 서비스 분석

- **급똥맵 (toiletmap.co.kr)** — 지도 기반, 로그인 불필요. 위치/시설 정보 중심.
- **대똥여지도** — 앱. 운영시간, 기저귀 교환대, 장애인 화장실, 즐겨찾기, 제보 기능 보유.
- **급해요 (geuphaeyo.app)** — 위치 기반 최단 화장실 안내, 운영시간/접근성 정보.
- **화장실지도** — 구글맵 GPS 기반, 음성 내비게이션.

공통점: 전부 "위치 + 운영시간" 수준의 화장실 찾기에 머물러 있음. 우리가 파고들 틈:
급한 사람의 UX(IBS 모드, 초단위, 원버튼 내비), 긴급 접근 팁, 계단 적은 경로, 아이 동반
관점, 그리고 약국 결합. 배울 점: 조회에 로그인을 요구하지 않는 것 (급한 사람에게 로그인은
장벽).

## 기술 스택

- React + Spring Boot (2026-07 모노레포 구성 확정: `frontend/`(Vite+React+TS),
  `backend/`(Gradle+Spring Boot))
- 프론트 스타일: **Tailwind CSS로 전환** (2026-08 최종 확정). 시각적 결과 자체는 plain
  CSS와 동일하게 낼 수 있지만, 카드 우선 UI 재설계 작업량이 커서 Tailwind의 빠른 반복
  작업 이점을 택함.
- 지도 API: **카카오맵 확정** (2026-08). 처음엔 네이버로 갔다가, 로그인·도보경로까지
  전부 카카오 생태계로 묶으려고 전환.
- 도보 경로 계산: **자체 호스팅 OSRM(foot 프로필)** 이 1순위 (2026-08-10 전환). 카카오모빌리티는
  도보 길찾기를 REST API로 제공하지 않아(URL Scheme으로 앱 딥링크만 지원) 처음엔 Tmap을
  택했으나, 무료 1,000건/일로는 서비스가 불가능해 자체 호스팅으로 옮김. 폴백 순서는
  **자체 OSRM → Tmap → 카카오 자동차 경로 → 직선거리**.
  - 서울만 잘라낸 OSM 데이터로 전처리한다(전국 272MB → 서울 41MB, 산출물 407MB, 구동
    메모리 약 280MB). 준비 절차는 README 참고.
  - 공개 데모 서버(router.project-osrm.org)는 쓰지 않는다 — 개발용이라 서비스 트래픽 금지고,
    foot 프로필이 자동차처럼 동작해 소요시간이 시속 34~54km로 나온다(실측).
- 주소→좌표 변환 / 장소 검색: 카카오 로컬 API (주소검색 + 카테고리검색). 지도 SDK와 같은
  카카오 앱 키를 재사용.
- GPS: 브라우저 Geolocation API.
- 공공데이터: data.go.kr 전국공중화장실표준데이터. 2025년 2월부터 좌표를 제공하지 않아
  카카오 주소검색으로 직접 geocoding해서 좌표를 채움. 서울시 데이터만 필터링해서 사용
  (MVP 지역 한정). 자동 동기화 API가 없어(구 지방행정인허가데이터개방시스템 폐지) 수동
  다운로드 → 배치 적재 방식으로 운영.
  - **geocoding 결과는 자치구로 검증한다** (2026-08-10). 카카오가 후보를 여러 건 주는데
    첫 건이 다른 구인 경우가 있어서, 입력 주소의 구와 일치하는 후보만 채택하고 없으면
    좌표를 비운다. 틀린 좌표는 좌표가 없는 것보다 나쁘다 — 엉뚱한 구로 안내하게 되고,
    실패는 로그에 남지만 오매칭은 아무 흔적도 남지 않기 때문.
  - 좌표 재계산은 옵션으로 둔다. 심야약국(38건)은 기본 on, 화장실(5,619건)은 카카오 호출량
    때문에 기본 off — 일괄 교정이 필요할 때만 켠다.
- 편의점/카페/대형마트 "이용 팁": 카카오 로컬 카테고리 검색으로 실시간 조회 (DB 저장 안 함).
- DB: **MySQL** (로컬은 Homebrew 직접 설치, JPA `ddl-auto`로 스키마 관리). 서울시 한정
  MVP 규모에서는 PostGIS 없이도 충분 — 전국 확장/대량 데이터 시점에 재검토.
- 즐겨찾기, 신고 기능, 이미지 업로드 — 아직 미구현.
- Redis (인기 화장실 캐싱), 랭킹, 관리자 페이지 — MVP 이후.

## 인증 / 데이터 설계

- **리뷰(별점)는 로그인 없이 누구나 작성 가능** (2026-08 변경). 원래는 카카오/구글
  소셜 로그인 + JWT로 리뷰 작성자를 인증할 계획이었으나, 로그인을 붙이는 비용 대비
  얻는 게 적다고 판단해 제거함. 스팸/어뷰징 방지는 추후 필요해지면 재검토.
  - 원칙은 유지: 조회(지도/화장실/약국 보기)는 로그인 없이 가능.
  - 즐겨찾기 등 "개인 계정에 귀속되는" 기능은 여전히 로그인이 필요할 수 있음 — 실제
    구현 시점에 다시 결정.
- **restrooms 테이블 (화장실 마스터)**: 우리가 발급하는 내부 id(기본키) + 공공데이터/네이버
  원본 ID는 external_id 컬럼에 별도 보관. 위치, 카테고리, 시설정보, status(활성/폐쇄) 포함.
  - 공공데이터 동기화 시 external_id로 매칭 → 기존 화장실은 정보만 업데이트, 새 화장실은
    새 내부 id로 추가, 소스에서 사라진 화장실은 삭제하지 않고 status를 "폐쇄"로 변경
    (기존 리뷰 보존 + "없어졌어요" 안내 가능).
  - **canonical_id 컬럼으로 원본 중복을 흡수한다** (2026-08-10). "external_id 하나 = 물리적
    장소 하나"라는 전제가 이 데이터셋에서는 깨진다 — 원본 CSV가 같은 시설을 서로 다른
    관리번호로 2~5번 수록하기 때문(같은 연도 안에서도, 연도판이 겹쳐서도). 5,619행 중
    669행이 중복이라 "지금 급해!" 상위 목록의 슬롯이 낭비됐다(예: 노량진역 화장실이
    상위 6개 중 3칸을 먹고 그중 2개가 동일 시설).
    - 판정 키는 **이름 + 좌표**. 주소는 넣지 않는다 — 같은 장소인데 괄호 안 건물명 표기만
      다른 경우가 41그룹 있어서 주소를 키에 넣으면 중복이 안 잡힌다. 좌표만 써도 안 된다 —
      노량진역(1)/(2)/(3)처럼 좌표가 같은 별개 화장실이 357그룹 있어 멀쩡한 곳까지 합쳐진다.
    - 대표는 그룹에서 **가장 작은 내부 id**로 고정. 재계산해도 대표가 바뀌지 않아야 리뷰가
      딸려 움직이지 않는다.
    - 중복 행은 지우지 않고 canonical_id가 대표를 가리키게 한다(위 "삭제하지 않는다" 원칙
      유지). 조회는 canonical_id가 null인 행만 본다. 리뷰는 읽기·쓰기 모두 대표 id로
      치환해 한 곳에 모은다.
    - 판정은 DB만 보는 별도 배치라 CSV 임포트와 독립적으로 재실행할 수 있다. 임포트로 새
      행이 들어온 뒤 한 번 돌리면 된다.
- **reviews 테이블**: review_id, 화장실 내부 id 참조, 별점, 이모지 평가(😀/😐/😱),
  체크박스 항목(휴지 있음/비데 있음/줄 없음/무료/깨끗함/비밀번호 없음), comment(자유
  텍스트, 최대 200자, nullable, 2026-08 추가), 작성일. 작성자 식별 없음(비로그인).
  리뷰는 항상 화장실 내부 id를 참조하므로 외부 데이터가 갱신돼도 리뷰가 끊기지 않음.
- **pharmacies 테이블 (전체 레이어)**: restrooms와 동일한 패턴 — 내부 id(기본키) + 공공데이터
  원본 ID는 external_id, 위치, 요일별(월~일) + 공휴일 영업시간, status. 서울시 약국
  운영시간 정보(서울 열린데이터광장 Open API)를 주 1회 배치 호출로 적재. "지금 영업 중"
  판정은 영업시간 데이터 기준으로 서버에서 계산 (2026-08 결정, 상세는 변경 이력 참고).
  - 좌표는 API가 주는 WGS84LAT/LON을 쓰되 **적재 전에 검증한다** (2026-08-10). 이 API는
    서로 다른 자치구의 약국에 같은 좌표를 주는 오류가 있다(174건). 같은 좌표를 공유하는
    약국들의 주소가 서로 다른 구를 가리키면 그중 최대 한 곳만 맞을 수 있으므로, 그 좌표는
    믿지 않고 주소 기준으로 다시 geocoding한다. 같은 건물에 입주한 약국끼리 좌표를
    공유하는 건 정상이라(한 건물에 10곳인 사례도 있음) 구가 갈리는 경우만 골라낸다.
- **night_duty_pharmacies 테이블 (야간 레이어)**: 서울시 공공심야약국(25개 자치구 39개소)
  전용. pharmacies와 매칭하지 않고 완전히 별개 소스로 취급(급똥 탭의 검증된 화장실 vs
  이용팁과 동일한 원칙). 내부 id + external_id + 위치 + 요일별 야간 운영 여부(월~일
  boolean) + 심야 시작/종료시간(대부분 22:00~익일 01:00) + status. 공식 API가 없어
  서울시 발표 자료를 수동 수집해 시드 데이터로 적재, 시가 새로 발표할 때만 수동 갱신
  (2026-08 결정).
- **데이터 구성 원칙**: 공공데이터로 기본 화장실/약국 리스트를 깔고, 그 위에 사용자
  리뷰·제보로 쌓이는 우리 데이터를 합쳐서 보여주는 하이브리드 구조. 평균 별점 등 집계값은
  Redis에 캐싱해서 조회 속도 확보.

## 화면 구조

- 상단 내비게이션 탭: **[급똥]** / **[약국]**
- 두 탭 모두 로그인 없이 바로 사용 가능.

### 디자인 철학

"가장 가까운 화장실을 가장 빨리 안내한다." 지도는 보조 역할일 뿐, 메인은 **행동**임.
모든 화면에서 사용자가 몇 초 안에 "여기로 가면 된다"를 이해할 수 있어야 함. 정보
우선순위: 도착 시간 > 거리 > 길찾기 버튼 > 이름 > 신뢰도 > 후기.

디자인 키워드: Minimal, Modern, 화이트 기반 + 충분한 여백 + 큰 타이포 + 라운드 카드 +
Soft Shadow. 참고 레퍼런스: Apple HIG, Toss, Kakao Mobility, Linear, Airbnb.

**브랜드 팔레트 (2026-08-07 확정)** — 휴지 마스코트 일러스트의 핑크 파스텔 톤 기반.
급함이라는 주제가 자칫 불쾌해질 수 있어, 귀여운 캐릭터 톤으로 중화하는 방향.

| 토큰 | 값 | 용도 |
|---|---|---|
| `cham-pink` | `#fadce0` | 소프트 핑크 — 배경/거터 |
| `cham-blush` | `#f5a8b5` | 블러시 — 그림자/보조 강조 |
| `cham-coral` | `#ef6b6b` | 코랄 — 마스코트 입, 강조/CTA |
| `cham-sky` | `#dceef8` | 라이트 블루 — 휴지 롤 |
| `cham-blue` | `#4a6fb5` | 딥 블루 — 롤 심지, 메인 액센트 |
| `cham-ink` | `#3f3a44` | 잉크 — 본문 텍스트 |

**탭별 액센트** — 같은 팔레트 안에서 탭 정체성을 구분한다 (2026-08-07 추가).
급똥 탭은 코랄, 약국 탭은 보라. 약국 보라는 약사 일러스트 레퍼런스에서 추출.

| 토큰 | 값 | 용도 |
|---|---|---|
| `cham-purple` | `#6b4fd8` | 약국 메인 액센트 (탭·필터칩·CTA·알약 캐릭터) |
| `cham-lilac` | `#ede8fb` | 보라의 옅은 면 (뱃지 배경, 선택된 리스트 행) |
| `cham-tangerine` | `#f0a73e` | 보조 강조. 채도가 높아 **작은 텍스트에는 쓰지 않고** 배경/보더로만 사용 |

**마스코트**: 탭별로 다른 캐릭터가 지도 경로를 따라 뛰어가는 애니메이션 (기존 💩🧻
이모지를 대체). 전부 인라인 SVG라 별도 에셋 파일 없음.

- 급똥 탭: `>_<` 표정(= "참는 중")의 휴지가 앞서 도망가고, `^^` 표정의 똥이 뒤쫓는 2인조.
- 약국 탭: 보라/화이트 캡슐 알약 캐릭터.
- 급똥 탭 캐릭터에는 핑크 네온 글로우(blush+coral 2겹, 1.1초 주기)를 얹어 강조.
  약국 알약은 네온 없이 차분하게 — 급함의 온도차를 표현.

### 급똥 탭 — 첫 화면 (2026-08 재변경: 리스트 우선 구조)

한때 "카드 1개만 크게" 방식(리스트 숨기고 최단거리 화장실 하나만 보여주는 원버튼 구조)으로
재설계했었으나, 다시 **리스트 우선 구조**로 되돌림 (2026-08). 이유: 급한 상황이라도
사용자가 몇 초 안에 2~3개 옵션 중 고를 수 있는 편이 실사용에 낫다고 판단.

- **좌측(또는 상단) 패널: "가장 가까운 화장실" 리스트.** 순위 번호(①②③...) + 이름 +
  카테고리 뱃지(편의점/카페/공공화장실 등) + 도보 시간(초단위, 색상 강조) + 거리(m) +
  운영시간 + 시설 태그(24시간·내부·남/여 분리 등) + 별점(리뷰 수 포함)을 한 줄 카드로
  나열. 1위 카드는 강조 테두리로 구분.
  - "더보기" 버튼으로 리스트 확장 (기본 6개 정도만 먼저 노출).
- **우측: 지도.** 리스트의 각 항목이 지도 위 번호 마커와 1:1로 매칭 (①②③...). 1위
  항목은 현재 위치 → 목적지까지 경로선 + 도보 소요시간/거리 라벨을 지도 위에 직접 표시.
  마커 색상으로 카테고리 구분 가능 (검증된 화장실 🔵 / 이용 팁 🟡 등 기존 원칙 유지).
  - 상단에 필터 탭: 전체 / 공공화장실 / 편의점 / 카페 / 더보기.
  - 지도 우측에 현재위치 버튼, 레이어 토글, 확대/축소 컨트롤.
  - 화면 하단(또는 지도 위)에 **[길찾기] 버튼이 항상 떠 있음** (floating CTA).
- **하단: "추천 화장실 상세" 카드.** 리스트에서 선택한(또는 1위) 화장실의 사진, 이름,
  카테고리, 운영시간/시설 요약, 시설 아이콘 태그(깨끗해요/냄새없어요/휴지있어요/
  핸드솝있어요 등), 별점 요약. 옆에 실시간 사용자 후기 2~3개(작성 시간 포함) 미리보기 +
  "더보기"로 전체 후기. 하단에 [즐겨찾기] [후기 작성] [길찾기] 버튼.
- 시간대별 색상은 🟢1분 이내 / 🟠3분 이내 / 🔴5분 이상 3단계 유지.
- 급똥지수는 리스트 카드와 상세 카드 모두에 노출 (별점 옆 또는 위에).
- 하단 탭바: 지도 / 목록 / 내 주변 / 즐겨찾기 / 마이페이지.

### 상세 페이지

사진(크게) → 급똥지수 → 평점 → 후기 → 시설정보 → 길찾기 순으로 배치. ("추천 화장실
상세" 카드와 동일한 정보 우선순위를 전체 화면으로 확장한 버전.)

### 약국 탭

지금 영업 중인 약국 지도. 급똥 탭과 동일하게 리스트+지도+상세 카드 구조, 도보 시간순
정렬과 경로 표시도 동일하게 적용. 상단 필터는 **전체** / **야간** + "지금 영업중" 토글로
구성 (2026-08 결정).

- **전체**: 서울시 약국정보 5,510곳. 요일별 등록 영업시간 기준으로 "지금 영업 중"을
  서버에서 계산.
- **야간**: 21시 넘어서까지 하는 일반약국 + 서울시 공공심야약국 38곳을 함께 표시.
  정부 공식 심야 기준은 22시지만, 서울시 약국 기준 21시가 지나면 열린 곳이 477곳(8.6%)
  까지 급감해 실제 공백은 21시부터 시작함. 22시로 잡으면 정작 밤 9시에 야간 필터를
  눌렀을 때 결과가 비어버려서 21시를 기준으로 삼음 (2026-08-07 결정, 실데이터 확인).
  참고 수치: 20시 1,373곳 / 21시 477곳 / 22시 119곳 / 23시 12곳 / 자정 이후 일반약국 0곳
  (자정 넘으면 공공심야약국 38곳이 유일한 선택지).
- 상세 카드: 도보 시간, 영업 상태, 전화 걸기, 네이버 리뷰 검색 링크, 길찾기. 영업시간이
  약국 자체 등록 정보라 실제와 다를 수 있다는 안내를 함께 노출.

데이터 설계는 위 "인증 / 데이터 설계" 섹션의 pharmacies / night_duty_pharmacies 참고.

## 이름

**참지마** (확정)

- 이유: 앱의 정체성("참지 말고 바로 찾아")을 그대로 전달, 기억하기 쉬움, 기존 경쟁
  서비스와 겹치지 않음.
- 탈락 후보 기록: 급똥맵(동일 이름 서비스 존재), 급똥여지도(경쟁 앱 "대똥여지도"와 혼동
  및 상표 분쟁 소지), 싸이렌, 푸세권, 변사또, 골든타임 등.

## 변경 이력

- 2026-07-22: 최초 작성 (GPT 초안 기반 정리)
- 2026-07-22: 캠핑장 정보 기능 삭제 (범위 축소 결정). 즐겨찾기 예시에서도 캠핑장 제외.
- 2026-07-22: "인증 / 데이터 설계" 섹션 추가 — 소셜 로그인+JWT, 내부 id/external_id 분리,
  status 관리, 공공데이터+자체 리뷰 하이브리드 구조.
- 2026-07-22: 경쟁 서비스 분석 섹션 추가 (급똥맵/대똥여지도/급해요/화장실지도). 조회는
  비로그인 원칙 명시.
- 2026-07-22: 앱 이름 "참지마" 확정. 약국 기능(지금 영업 중인 심야/휴일 약국) 추가,
  상단 [급똥]/[약국] 탭 화면 구조 추가, pharmacies 테이블 설계 추가.
- 2026-07-22: 포지셔닝 섹션 추가 — 주: 급함 / 보조: 안심(아이와 함께 모드, 안심 화장실
  뱃지).
- 2026-07~08: React(Vite)+Spring Boot 모노레포 뼈대 구축, MySQL 연결, 서울시 화장실
  공공데이터 5,619건 적재(좌표는 카카오 geocoding으로 직접 확보), 반경 검색 API, 지도에
  현재위치/검증된 화장실(🔵)/이용 팁(🟡, 편의점·카페·대형마트) 표시, 지도 상단 장소 검색.
- 2026-08: 지도 API를 네이버에서 **카카오맵**으로 전환. 도보 시간 계산은 카카오모빌리티가
  REST API를 제공하지 않아 **Tmap 보행자 경로 API**로 확정. "지금 급해!"가 검증된
  화장실 + 편의점/카페/마트 팁을 도보시간 기준으로 함께 정렬하도록 구현(포함 여부는
  결과창에서 토글 가능).
- 2026-08: 리뷰(별점) 기능을 **로그인 없이 누구나 작성 가능**하도록 변경. 원래 계획이던
  카카오/구글 소셜 로그인 + JWT는 붙이는 비용 대비 얻는 게 적다고 판단해 보류.
- 2026-08: UI/UX 전면 재설계 방향 확정 — 리스트 우선이 아니라 "가장 가까운 화장실 카드
  1개"를 첫 화면에 크게 보여주는 구조로 전환. 급똥지수(거리/청결/휴지/후기/혼잡도/성공률
  종합 점수) 개념 추가, 리뷰는 이모지+체크박스로 3초 안에 작성, 도착시간 색상 3단계
  (🟢🟠🔴)로 단순화, 하단 floating [길찾기] 버튼 상시 노출. 프론트 스타일은 Tailwind
  CSS 기반으로 전환 예정(기존 plain CSS에서 마이그레이션 필요, 진행 여부 확인 중).
- 2026-08: 화면 구조를 **리스트 우선 구조로 재변경** — 위의 "카드 1개만 크게" 결정을
  되돌림. UI 시안 이미지(좌측 순위 리스트 + 우측 지도 + 하단 상세 카드 레이아웃)를
  기준으로 급똥 탭 화면 구조 재정리. 급똥지수/색상 3단계/floating 길찾기 버튼 등 앞서
  확정된 요소는 유지한 채 리스트+지도+상세카드 레이아웃에 맞게 배치만 조정.
- 2026-08: 리스트+지도+상세카드 구조 실제 구현 완료 (UI 목업 `frontend/src/mockups/`
  기준). 지도 마커를 순위 번호+색상(🟢🟠🔴) 뱃지로 교체. 목업과 다르게 처리한 부분 2개:
  (1) 시설 사진은 실제 사진이 아닌 목업의 스톡 이미지라 그대로 쓰면 사용자를 속이는
  셈이라, 카테고리 아이콘(🚻/🏪/☕) placeholder로 대체. (2) 후기 텍스트 미리보기는
  우리 리뷰 데이터가 이모지+체크박스뿐이라(자유 텍스트 없음), 기존에 만든 👍/👎 태그
  요약으로 대체. 하단 탭바(지도/목록/즐겨찾기/마이페이지)와 [즐겨찾기] 버튼은 로그인·
  저장소 설계가 안 끝나서 이번 라운드에서 제외.
- 2026-08: 급똥 탭을 **지도 전체화면 + 플로팅 UI** 구조로 개편 — 지도가 배경을 꽉 채우고,
  검색창/필터/상세카드/리스트가 전부 그 위에 뜨는 카드로 분리됨(구글맵 스타일). 길찾기
  버튼에 "지도에서 길찾기"(우리 지도에서 경로 바로 확인)와 "카카오맵 상세 길찾기"(외부
  카카오맵 앱으로 실제 내비게이션, 출발지를 현재 위치로 명시) 두 가지 제공. 리뷰 등록 후
  상세카드에 바로 반영 안 되던 버그 수정.
- 2026-08: 리스트 항목 선택 시 지도에 경로+💩🧻 캐릭터가 자동으로 뛰어가는 애니메이션
  추가 (별도 버튼 없이). "지도 경로" 버튼은 제거하고 카카오맵 링크는 상세카드 이름
  옆 아이콘으로 이동, 후기 작성을 주 액션으로 승격. 도보 시간 API는 **Tmap → OSRM
  무료 공개서버(도보 프로필) → 카카오 자동차 경로 → 직선거리** 4단계 폴백으로 강화 —
  Tmap 무료 할당량(1,000건/일)이 개발 중 자주 소진되는 문제 대응. 후보 수도
  18→12건으로 줄여 할당량 소진 속도 완화. 급똥 탭 기본 필터를 "전체"→"공공화장실"로
  변경.
- 2026-08-03: 리뷰에 **자유 텍스트 코멘트(선택, 최대 200자)** 추가 — 앞서 "3초 룰" 때문에
  자유 텍스트 없이 이모지+체크박스만 두기로 했던 결정(위 UI 목업 구현 항목 참고)을 사용자
  요청으로 재변경. 필수 항목이 아니므로 3초 룰 자체는 유지. reviews 테이블에 comment
  컬럼 추가, 상세 카드에 코멘트 있는 최근 리뷰 최대 3개를 이모지+본문+작성 시간으로
  미리보기.
- 2026-08-06: 급똥 탭 UI 다듬기 — 후기작성 버튼 높이 증가, 필터 줄(전체/공공화장실/
  편의점/카페/아이와 함께)에 **주유소(카카오 OL7)** 팁 카테고리 추가, 모바일 레이아웃을
  "지도 전체화면 + 하단 리스트(footer 고정) + 그 위에 상세카드가 동일 크기로 덮이는"
  구조로 재정리(리스트/상세카드가 화면을 다 차지하던 문제 해결). 필터 줄은 리스트와
  동일한 폭으로 늘리고 버튼은 flex-1로 균등 분배.
- 2026-08-06: **약국 탭 데이터 설계 확정.** 화장실처럼 리서치·인터뷰를 거쳐 결정:
  (1) 전체(평소 영업)와 야간(공공심야약국) 하이브리드 두 레이어로 구성하되 서로 매칭하지
  않고 별개 소스로 취급(급똥 탭의 검증된 화장실 vs 이용팁 패턴 재사용). (2) 전체 레이어는
  서울시 약국 운영시간 정보(서울 열린데이터광장 Open API, 인증키 발급 완료)를 주 1회
  배치 호출해 pharmacies 테이블에 적재. (3) 야간 레이어는 처음엔 E-GEN 공공심야약국
  API를 검토했으나 해당 API가 실제로 존재하지 않는 것으로 확인되어 폐기, 대신 서울시
  공공심야약국이 25개 자치구 39개소뿐인 소규모 고정 리스트라는 점을 확인하고 API 연동
  없이 시 발표 자료를 수동 수집해 night_duty_pharmacies 테이블에 시드 데이터로 적재하는
  방식으로 결정(시가 새로 발표할 때만 수동 갱신). pharmacies/night_duty_pharmacies
  엔티티·리포지토리 구현 완료.
- 2026-08-07: **약국 탭 구현 완료.** 데이터 적재(전체 5,510곳 API 배치 / 공공심야약국 38곳
  수동 시드, 좌표 100% 확보), "지금 영업중" 계산, 급똥 탭과 동일한 리스트+지도+상세카드
  UI. 추가 결정 3가지: (1) **"야간" 기준을 22시 → 21시로 변경** — 공식 심야 기준은 22시지만
  실데이터상 공백은 21시부터 시작하고, 22시로 잡으면 밤 9시에 야간 필터가 빈 화면이 되는
  문제가 있어서. (2) **약국에도 도보 시간/경로 안내 적용** — 급똥 탭의 4단계 폴백 로직을
  `WalkingRouteService`로 분리해 두 탭이 공유. (3) 상세 카드에 **네이버 리뷰 검색 링크**
  추가(별점 수치는 Place Detail API 연동 전까지 표시하지 않음).
- 2026-08-07: 상단 탭 명칭/스타일 변경 — "급똥" → **"참지마(급똥)"**, 탭 아이콘 🚽 → 🧻,
  선택된 탭을 배경 음영 + 라운드 타일로 차별화, 글자 크기 확대.
- 2026-08-07: **브랜드 비주얼 방향 확정 — 핑크 파스텔 + 휴지 마스코트.** 레퍼런스 3종
  (Dribbble "Toilet Papers", 캐러멜 브랜드 아이덴티티, `>_<` 표정 휴지 캐릭터)을 검토해
  핑크 파스텔 톤으로 결정. 중간에 캐러멜의 다크 마룬+앰버 조합도 적용해봤으나, 마스코트가
  흰 종이라 핑크 배경이 더 잘 받아서 교체. 팔레트를 Tailwind 테마 토큰(`cham-*`)으로
  정의(위 디자인 철학 섹션 표 참고), 상단 탭에 우선 적용. 지도 위 카드/버튼/필터 칩 등
  나머지 파란색 계열 UI는 아직 미적용 — 후속 작업.
- 2026-08-07: **약국 탭 비주얼 확정 — 보라 액센트 + 미니멀 카드.** 레퍼런스 2종(약사
  일러스트의 보라/오렌지, hims 앱의 미니멀 베이지 레이아웃)을 합쳐서 적용. 보라를 약국
  전용 액센트로 삼아 급똥 탭(코랄)과 구분하고, 카드·리스트는 hims처럼 여백을 넓히고
  볼드/라이트 폰트 대비로 정리(약국명은 bold, 영업시간·거리 등 메타는 light). 알약
  캐릭터도 보라로 교체. 급똥 탭은 핑크/코랄 유지.
  - 시간 표시 색 처리: 리스트와 지도 마커는 기존 3단계 색(🟢🟠🔴)을 유지해 여러 곳을
    비교할 때 가까운 순서가 한눈에 보이게 하고, 상세 카드는 단일 항목이라 비교 맥락이
    없고 약국은 급똥만큼 긴급하지 않아 보라로 통일함.
- 2026-08-10: **공공데이터 품질 대응 3건.** 실사용 중 "지금 급해!" 목록에 같은 화장실이
  두 번 뜨는 걸 발견해 추적한 결과, 셋 다 원본 데이터 문제로 수렴했다(임포터 코드는 정상).
  (1) **화장실 중복 669행** — 원본 CSV가 같은 시설을 여러 관리번호로 수록. `canonical_id`
  컬럼을 추가해 대표 행으로 묶고 조회에서 중복을 제외(5,619행 → 노출 4,950행). 판정 키와
  대표 선정 근거는 위 데이터 설계 섹션 참고. 기존 리뷰 6건은 전부 대표 행에 있어 이전
  작업 불필요했음. (2) **약국 좌표 174건** — 서울시 API가 다른 구 약국에 같은 좌표를 부여
  (예: 서초구 가온약국이 은평구 좌표를 받음). 적재 시 검증 후 주소 기준 재계산하도록 수정,
  174건 전부 교정 완료(실패 0건). (3) **geocoding 오매칭** — 카카오 후보 중 첫 건을 검증
  없이 채택하던 것을 자치구 일치 검증으로 교체, 좌표가 한 번 들어가면 영원히 갱신되지 않던
  문제도 옵션으로 해소. 단위 테스트 15개 추가.
  - 기록 정정: 위 2026-07~08 항목의 "5,619건 적재"는 중복 669행을 포함한 수치이고, 실제
    고유 화장실은 4,950곳이다. 2026-08-07 항목의 약국 "좌표 100% 확보"도 커버리지 기준이며
    그중 174건은 좌표가 틀렸었다.
  - 남은 이슈: 화장실 200행은 아직 좌표가 없어 지도에 안 나온다(초기 geocoding 실패분).
    별도 확인 필요.
- 2026-08-10: **이용 팁 레이어 중복 제거.** 카카오에도 같은 가게가 표기만 다르게 두 번
  등록된 경우가 있어("% 아라비카 여의도 63빌딩점" / "퍼센트아라비카 여의도63빌딩점" — 장소
  ID만 다르고 주소·좌표·카테고리가 소수점까지 동일) 목록에 나란히 떴다. 팁 레이어는 DB에
  저장하지 않는 실시간 조회라 canonical_id를 쓸 수 없어 조회 시점에 거른다.
  - 판정은 **카테고리 + 좌표 + 이름 포함 관계** 세 조건을 모두 만족할 때만. 좌표만 쓰면
    안 된다 — 같은 좌표의 다른 지점("데블다이스 강남4호점" / "강남시티점", 실측)이 합쳐진다.
    화장실의 노량진역(1)/(2)/(3)과 같은 함정.
  - 남길 이름은 정규화 후 **짧은 쪽**. 중복 표기는 브랜드 마크를 풀어 쓴 형태(`%`→`퍼센트`)라
    항상 더 길어서, 짧은 쪽이 간판의 공식 표기와 일치한다.
  - 빈도는 낮다(카카오 350곳 샘플에 1쌍). 다만 63빌딩 카페처럼 목록 상위에 뜨면 눈에 띈다.
- 2026-08-10: **도보 경로를 자체 호스팅 OSRM으로 전환.** 일반 공개를 검토하면서 외부 API
  할당량이 막힘을 확인해서 결정. "지금 급해!" 한 번에 후보 12곳(화장실 8 + 팁 4)의 경로를
  각각 호출하므로 Tmap 무료 1,000건/일은 **하루 83요청**이면 소진된다 — 공개 서비스로는
  성립하지 않는 수치.
  - 경쟁 서비스 급똥맵을 조사해보니 **도보 시간을 아예 계산하지 않는다**(지도 영역 기준
    마커 조회 + 줌별 클러스터링만). 그래서 전국 5만 개를 싸게 커버한다. 우리의 차별점인
    "초 단위 도보 시간"이 곧 우리의 비용 구조라는 게 확인됐고, 그렇다면 그 비용을 외부
    할당량이 아니라 자체 인프라로 감당하는 게 맞다고 판단.
  - 전환 결과: 응답 12건 **0.33초**(순차 호출인데도 로컬이라 충분히 빠름 — 병렬화 불필요),
    할당량 무제한, 소요시간이 시속 5km로 정확해짐.
  - **거리와 순서가 실제로 바뀌었다.** 보행자 전용길·지름길이 반영되면서 여의동주민센터는
    859m→745m로 줄고 노량진역은 2,860m→3,322m로 늘었다(차도로는 못 가는 구간 우회).
    목록 순서도 바뀜(맥도날드 12위→9위). 경로선도 실제 보행로를 따라 그려진다.
  - 검토했다가 접은 안: `/table`(목적지 여러 개를 1회 호출)은 외부 할당량을 아끼는 수단인데,
    자체 호스팅으로 그 제약이 사라졌고 경로선을 안 줘서 프론트 수정이 필요해 채택하지 않음.
    도보 경로 DB 캐싱도 접음 — 출발지가 GPS 연속값이라 히트율이 낮고, 좌표를 격자로 뭉개면
    "초 단위 정확도" 컨셉과 충돌한다.
- 2026-08-10: **무료 배포 조합 확정 — 프론트 Render Static Site + 백엔드 Google Cloud Run +
  DB Neon(Postgres).** Render 무료 웹서비스(15분 후 중지, 콜드스타트 약 1분)와 Fly.io(무료
  티어 폐지, 2시간 체험판뿐)를 공식 문서로 확인 후 제외. Cloud Run은 월 2백만 요청 무료지만
  카드 등록이 필수고 한도 초과 시 자동 과금되는 유일한 컴포넌트 — 예산 알림 설정 권장.
  - DB는 MySQL 관리형 무료가 마땅치 않아(PlanetScale 무료 종료, Aiven은 유휴 시 꺼진 뒤
    자동으로 다시 켜지는지 문서에 불명확) Postgres로 전환. 코드에 네이티브 SQL이 없어
    드라이버만 추가(`build.gradle`에 MySQL·Postgres 둘 다 유지, JDBC URL 스킴으로 자동
    선택 — 로컬 개발은 MySQL 그대로, 배포는 Postgres). Neon은 카드 불필요·무기한 무료,
    유휴 5분 후 슬립하지만 다음 요청에 **수백 ms 만에 자동 기상**(공식 문서 확인) — Aiven과
    달리 수동 개입이 필요 없어서 택함.
  - 백엔드는 Spring Boot(JVM)와 OSRM을 **한 컨테이너**에 담는다. Cloud Run은 서비스 단위로
    무료 한도가 매겨져서 둘로 쪼개면 한도 소진이 빨라진다. 최종 이미지 베이스는 OSRM 공식
    이미지(`ghcr.io/project-osrm/osrm-backend`)를 그대로 쓰고 그 위에 Eclipse Adoptium
    공식 API로 받은 JRE 25 타르볼을 얹는 방식 — osrm-routed가 요구하는 공유 라이브러리를
    추측해서 옮기다 런타임에 깨질 위험을 피하기 위함(로컬에 Docker가 없어 직접 빌드
    검증이 불가능했던 제약 때문에 더 보수적으로 설계).
  - **콜드 스타트는 무료인 이상 못 피한다.** 로컬 실측: OSRM 기동 0.3초, Spring Boot jar
    기동 2.8초(OS 캐시 남은 상태라 실제보다 빠를 수 있음). 15분 이상 방치 후 첫 요청은
    최소 몇 초, "3초 안에" 카피와는 타협이 있는 지점 — Render 무료의 1분보다는 낫고
    트래픽이 늘수록 옅어진다.
- 2026-08-10: **실제 배포 완료.** 프론트 https://chamjima-frontend.web.app (Firebase
  Hosting), 백엔드 https://chamjima-backend-412068365628.asia-northeast3.run.app
  (Cloud Run, 프로젝트 `chamjima-app`), DB는 Neon(Postgres). 로컬 MySQL의 정제된
  데이터(중복 제거된 화장실 4,950곳, 좌표 검증된 약국 5,510곳, 리뷰 6건 — 총 11,173행)를
  테이블별 TSV로 그대로 이관해서 카카오 geocoding 재호출 없이 완료. 배포 직후 실측
  콜드 스타트는 헬스체크 기준 약 1초 — 우려했던 것보다 훨씬 나음.
  - **프론트는 계획을 바꿔 Render 대신 Firebase Hosting(Spark, 무료)으로 배포했다.**
    Render의 Blueprint 배포 흐름이 무료 정적 사이트에도 결제 정보 등록을 요구하는 걸
    실제 화면에서 확인(공식 문서의 "카드 불필요"와 실제 동작이 다름). Firebase Spark는
    카드 자체를 요구하지 않는 게 공식 확인되고, 이미 있던 구글 계정(Cloud Run용)을
    그대로 재사용할 수 있어 새 계정도 안 만들어도 됐다. `render.yaml`은 삭제.
  - 결제 계정이 이 구글 계정에 하나도 없어서(Cloud Run 필수 조건) 사용자가 콘솔에서
    직접 만들어야 했음 — 이건 CLI로 대신할 수 없는 유일한 수동 단계였다. 나머지
    (프로젝트 생성, API 활성화, 이미지 빌드·배포, 데이터 이관, DNS/CORS 설정)는 전부
    CLI로 진행. 로그인도 브라우저 접속 자체는 사용자가 했지만, 인증 코드를 대신
    받아 gcloud/firebase CLI에 전달하는 방식으로 자동화(각각 device-code 플로우).
  - 카카오맵 SDK는 배포 도메인을 카카오 개발자센터에 등록해야 뜬다 — 이것도 로그인이
    필요해 사용자 몫으로 남음. 등록 전까지 앱은 "카카오맵 SDK 로드 실패"를 명확히
    보여주고 나머지 기능(탭·필터·API)은 정상 동작 — 지도 실패가 앱 전체를 막지 않게
    설계돼 있었던 덕분에 배포 검증이 수월했다.
